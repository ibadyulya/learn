# React — задачи

Отвечай своими словами, потом сверяйся с [01-react.answers.md](./01-react.answers.md).

> 🌐 English version: [01-react.tasks.md](../en/01-react.tasks.md)

---

## Вопрос 1
Что означает «UI = f(state)» и чем декларативный подход React отличается от
императивной работы с DOM?

## Вопрос 2
Что такое virtual DOM и reconciliation? Зачем в списках нужен `key` и почему индекс
массива — плохой ключ?

## Вопрос 3
Что делает `useEffect`? Для чего массив зависимостей и функция очистки? Приведи пример
утечки без cleanup.

## Вопрос 4
В чём разница между `useMemo`, `useCallback` и `useRef`? Когда что применять?

## Вопрос 5
Почему `setCount(c => c + 1)` часто предпочтительнее `setCount(count + 1)`? Как это
связано с замыканиями?

## Вопрос 6
Как уменьшить лишние ре-рендеры? Почему для `React.memo` важна неизменяемость данных?

## Вопрос 7
Что такое «stale closure» в хуках и как его избежать?

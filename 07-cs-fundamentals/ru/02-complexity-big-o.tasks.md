# Сложность и Big-O — задачи

Отвечай своими словами, потом сверяйся с [02-complexity-big-o.answers.md](./02-complexity-big-o.answers.md).

> 🌐 English version: [02-complexity-big-o.tasks.md](../en/02-complexity-big-o.tasks.md)

---

## Вопрос 1
Зачем оценивать алгоритм в Big-O, а не в секундах?

## Вопрос 2
Упрости по правилам Big-O: O(2n + 5), O(n² + n), O(n/2), O(100).

## Вопрос 3
Расставь по возрастанию: O(n log n), O(1), O(2ⁿ), O(n), O(log n), O(n²). Приведи пример на
каждый.

## Вопрос 4
Какая сложность у этого кода и почему?
```js
for (let i = 0; i < n; i++)
  for (let j = 0; j < n; j++)
    doSomething(i, j);
```

## Вопрос 5
Почему бинарный поиск — O(log n)? Что именно происходит на каждом шаге?

## Вопрос 6
Что такое амортизированная сложность? Почему `push` в динамический массив — амортизированно
O(1), хотя иногда O(n)?

## Вопрос 7
В чём разница между временной и пространственной сложностью? Приведи пример трейд-оффа.

# Замыкания и область видимости — задачи

Отвечай своими словами, потом сверяйся с [04-closures-scope.answers.md](./04-closures-scope.answers.md).

> 🌐 English version: [04-closures-scope.tasks.md](../en/04-closures-scope.tasks.md)

---

## Вопрос 1
Что такое лексическая область видимости? Функция берёт переменные из места вызова
или места объявления?

## Вопрос 2
Дай определение замыкания в одну-две фразы. Почему `count` в `makeCounter` не
исчезает после того, как функция вернула значение?

## Вопрос 3
Что напечатает и почему? Как получить `0, 1, 2`, не меняя `setTimeout`?
```ts
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}
```

## Вопрос 4
Реализуй `once(fn)` — обёртку, которая вызывает `fn` только при первом вызове, а
дальше возвращает закэшированный результат. Где здесь замыкание?

## Вопрос 5
Как с помощью замыкания сделать «приватную» переменную, недоступную снаружи?
Приведи мини-пример модульного паттерна.

## Вопрос 6
Каждый вызов `makeCounter()` даёт независимый счётчик. Почему они не делят `count`?

## Вопрос 7
Как замыкания связаны с утечками памяти? Приведи пример, где замыкание нечаянно
держит живым большой объект.

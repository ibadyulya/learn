# Типы, приведение и равенство — задачи

Отвечай своими словами, потом сверяйся с [07-types-coercion-equality.answers.md](./07-types-coercion-equality.answers.md).

> 🌐 English version: [07-types-coercion-equality.tasks.md](../en/07-types-coercion-equality.tasks.md)

---

## Вопрос 1
Назови 7 примитивов. Чем принципиально отличается копирование примитива и объекта?

## Вопрос 2
Почему `{a:1} === {a:1}` — это `false`? А как тогда сравнить два объекта по
содержимому?

## Вопрос 3
В чём разница между `==` и `===`? Приведи два примера, где `==` даёт
неожиданный результат из-за приведения.

## Вопрос 4
Перечисли все falsy-значения. Что из этого truthy: `'0'`, `''`, `[]`, `{}`, `0`,
`'false'`?

## Вопрос 5
Объясни результат каждого:
```ts
1 + '2'      // ?
'5' - 2      // ?
1 + 2 + '3'  // ?
'3' + 2 + 1  // ?
```

## Вопрос 6
Почему `NaN === NaN` — `false`? Как правильно проверить, что значение — `NaN`?

## Вопрос 7
Разница между `null` и `undefined`: когда появляется каждое? Что вернёт
`null == undefined` и `null === undefined`?

## Вопрос 8
Чем `Object.is` отличается от `===`? Назови два случая, где они расходятся.

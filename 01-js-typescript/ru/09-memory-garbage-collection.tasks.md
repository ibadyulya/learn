# Память и сборка мусора — задачи

Отвечай своими словами, потом сверяйся с [09-memory-garbage-collection.answers.md](./09-memory-garbage-collection.answers.md).

> 🌐 English version: [09-memory-garbage-collection.tasks.md](../en/09-memory-garbage-collection.tasks.md)

---

## Вопрос 1
По какому критерию GC решает, что объект можно собрать? Что такое «корни» (GC roots)?

## Вопрос 2
Опиши алгоритм mark-and-sweep в двух шагах.

## Вопрос 3
Соберётся ли пара объектов, которые ссылаются друг на друга, но больше ни на что не
завязаны? Почему счётчик ссылок здесь дал бы неверный ответ?

## Вопрос 4
Что такое утечка памяти в терминах достижимости? Приведи три типовых источника утечек
в JS.

## Вопрос 5
Почему это утечка и как починить?
```ts
const cache = {};
function remember(id, data) { cache[id] = data; }   // и никто не чистит
```

## Вопрос 6
Чем `WeakMap` отличается от `Map` с точки зрения GC? В каком сценарии он спасает от
утечки?

## Вопрос 7
Найди утечку и устрани её:
```ts
class Widget {
  constructor() {
    this.timer = setInterval(() => this.tick(), 1000);
  }
  tick() { /* ... */ }
  destroy() { /* удаляем виджет */ }
}
```
